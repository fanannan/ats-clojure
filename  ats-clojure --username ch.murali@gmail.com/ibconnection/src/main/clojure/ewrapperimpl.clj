
(ns ewrapperimpl
(:import (com.ib.client EWrapper))
)

(comment
(defrecord Ewrapperimpl  
  EWrapper 
  (error [this e] ) 
	;(error[this id errorCode errorMsg])
  ;(connectionClosed)
  ;(tickPrice [this tickerId, field, price, canAutoExecute])
  ;(tickSizeint [this tickerId field size])
  ;(tickOptionComputation [this tickerId field impliedVol delta modelPrice pvDividend]) 
  ;(tickGeneric[this tickerId tickType value]) 
  ;(tickString [this tickerId  tickType  value])
  )

)